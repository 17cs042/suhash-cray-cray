#include<stdio.h>
#include<conio.h>
void main()
{
string name[20];
printf("\n enter the name:");
scanf("%s,name);
if
{
printf("%s is a alphabet",name);
}
else
{
printf("%s is not a alphabet",name);
}
getch();
}
